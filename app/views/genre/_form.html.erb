<%= form_with model: genre do |f|%>

  <div class="field">
    <%= f.label :name, "ジャンル名" %></br>
    <%= f.text_field :name%>
  </div>

  <div class="submit">
    <%= f.submit %>
  </div>
  <% end %>